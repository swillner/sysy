#!/usr/bin/env bash
# -*- mode: sh; -*-

command=$1
shift

case "$command" in
    desc)
        echo
        ;;

    install)
        pip2 install --user "$@"
        ;;

    sync)
        pip2 install --user "$@"
        ;;

    uninstall)
        pip2 uninstall "$@"
        ;;

    update)
        pip2 install --user --upgrade "$@"
        ;;

    *)
        echo "Module does not support this command" 1>&2
        exit 1
        ;;

esac
